// 1. Entrada: El dato que vamos a comprobar
let contraseña = "cabestro"; // <-- Puedes cambiar esto para probar

// 2. Proceso (La Decisión):
if (contraseña.length >= 8) {
   // 3. Salida si es Válida
   console.log("Válida");
} else {
   // 3. Salida si No es Válida (si es 8 o menos)
   console.log("Rechazada");
}